//Create the variables firstName, lastName and fullName.
//Assign them a value and print in the console the full name

let firstName = "José Ángel";
const lastName = "Calero M.";

console.log(firstName.toLowerCase());
const fullName = firstName + " " + lastName;

console.log(fullName);

//Add and enter space directly
console.log(firstName + "\n" + lastName);

//The same as before but directly
console.log(`${firstName}
${lastName}`);

//The same as before but directly
console.log(`${firstName} ${lastName}`);
